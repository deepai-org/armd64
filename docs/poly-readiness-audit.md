# Poly Readiness Audit

Date: 2026-06-20

Objective: finish the ISA, Bochs emulator, and userspace runtime enough to have
an FPGA/silicon-ready Poly design for existing precompiled x86_64, AArch64, and
RISC-V64 code.

## Current Evidence

| Requirement | Evidence |
| --- | --- |
| Direct foreign fetch, not per-instruction traps | `docs/poly-isa.md`; Bochs raw AArch64/RISC-V decode in `bochs/cpu/proc_ctrl.cc`; `boot-poly-full-real-xsave-arch-traps` passed. |
| Dedicated Poly control operations | `docs/poly-isa.md`; CPUID geometry in `tools/include/polycpuid.h`; Bochs x86/AArch64/RISC-V control decode in `bochs/cpu/proc_ctrl.cc`. |
| Active ISA v2 proposal | `docs/poly-isa-userspace-offload-proposal.md` now carries the production v2 direction: keep canonical event frames required, and move memory probing, state derivation/import, event/result completion, register/debug-note export, and shared-memory ordering into OS-neutral ISA primitives. `docs/poly-isa-v2-draft.md` remains historical context for the descriptor-backed auto-spill prototype. |
| FPGA/silicon-ready ISA boundary | `docs/poly-isa.md` now has an explicit FPGA/silicon ISA readiness boundary covering x86 system ownership, native user-mode foreign fetch, fixed-latency decoded controls, CPUID-discovered opcode geometry, register-only `PCALL` signatures, canonical event frames, OS-neutral state/debug/probe primitives, return-cookie semantics, pre-mutation validation, x86-TSO memory ordering, and the separation between ISA readiness and RTL/FPGA implementation work. `make check-poly-contracts` verifies that boundary together with import selector stability, Bochs/runtime architecture wiring, CPUID/header constants, and state layout. |
| v2 feature discovery discipline | `tools/include/polycpuid.h` and Bochs expose `POLY_CPUID_V2_IMPLEMENTED_FEATURES`/`BX_POLY_CPUID_V2_IMPLEMENTED_FEATURES` so CPUID leaf `POLY_CPUID_BASE + 10` advertises only production v2 behavior: event frames, `PDUMP_STATE` debug-note export, `PMEM_PROBE_RANGE` non-faulting memory accessibility probes, `PDERIVE_STATE` thread-state derivation/import, `PCOMPLETE_EVENT` trap-result completion, `PFENCE` shared-memory ordering, the hardware monitor-entry stack/frame contract, and policy preflight for seccomp-style userspace mediation. The old spill descriptor/auto-spill trampoline implementation has been removed from `polyexec`, public CPUID, the RTL CPUID ROM, and the Bochs control path. `polyexec` builds its default trap-vector dispatch packet directly from the canonical v2 event frame; retired monitor-memory controls are no longer advertised through CPUID. `PDUMP_STATE` exports the OS-neutral note blob; `polyexec` can opt into fatal-fault ELF core artifact synthesis with `POLYEXEC_FATAL_DEBUG_NOTE_DIR`, using `PDUMP_STATE` for the live monitor context or the validated fatal spill state when the monitor cannot execute a live dump control. For fatal single-thread cores, `polyexec` now writes Linux ELF64 `ET_CORE` files with `NT_SIGINFO`/`NT_PRSTATUS`/`NT_PRPSINFO`/`NT_AUXV`, main-executable `NT_FILE`, executable and stack `PT_LOAD` segments, and the vendor `POLY` note; `boot-poly-gdb-core-validation` verifies `gdb-multiarch` can load AArch64/RISC-V cores and display guest registers, mapped executables, and symbolized fatal frames. Broader multi-object `NT_FILE` enumeration and perfect unwinds remain future runtime work above the OS-neutral v2 blob. `PMEM_PROBE_RANGE` reports canonicality, translation, read/write/execute permissions, page-crossing/alignment diagnostics, and permission-vs-translation failure classes without invoking guest OS fault handling. `PDERIVE_STATE` copies and patches AArch64/RISC-V child state images without naming clone/fork/vfork. `polyexec` requires advertised `PCOMPLETE_EVENT` for trap-result application, uses `PDERIVE_STATE` activation for clone/signal/sigreturn import paths, registers the hardware monitor-entry stack/frame contract instead of a naked trap trampoline, and places `PFENCE` around io_uring/shared-ring host syscall handoff. It intercepts guest seccomp filter installation, keeps cBPF filters in userspace, evaluates them against guest syscall metadata before host syscall translation, and the boot syscall gate requires an AArch64 policy fixture that blocks `mkdirat` with `EPERM`. Hardware ABI-descriptor caching and diagnostic-counter CPUID bits remain unadvertised until their emulator/runtime paths and regression tests are present. `polyexec`, `nativecheck`, `boot-poly-gdb-core-validation`, `scripts/checks/check_poly_cpuid_contract.sh`, and `scripts/checks/check_poly_arch_contract.sh` gate this. |
| Opcode discovery and ownership contract | `tools/include/polycpuid.h` defines CPUID escape subleafs 32 and 33 for active x86 opcode geometry and opcode-contract flags; Bochs returns both leaves; runtime probes validate both leaves; `rtl/poly_cpuid_rom.sv` returns the same hardware discovery subleafs and `rtl/poly_frontend_core.sv` exposes the Poly CPUID ROM through a hardware discovery sideband. `docs/poly-isa-v2-draft.md` defines production ownership behind that discovery contract: the current `0f 3a fc <subop>` page remains vendor/prototype and production-reassignable, while silicon must advertise a vendor-owned architectural extension page or a compatible vendor-owned production page through CPUID and must not use a `#UD` envelope. |
| AArch64/RISC-V extension baseline | `docs/poly-isa-v2-draft.md` states that AArch64 and RISC-V64 support is a defined baseline plus precise traps for unsupported, illegal, unavailable, or implementation-reserved foreign instructions; it does not promise a full implementation of every AArch64/RISC-V extension. |
| Zero-kernel-change state contract | `docs/poly-isa.md` and `docs/poly-isa-userspace-offload-proposal.md` document the active direction: canonical event frames remain the required monitor contract, while hardware should add OS-neutral memory probing, state derivation/import, event completion, register-note export, and shared-memory ordering so `polyexec` does not need a raw interrupt-time trampoline to emulate OS behavior. The old spill descriptor path was removed because Postgres exposed that raw page-fault entry can see a different address-space context than the interrupted frontend. The legacy restore opcode and auto-spill resume trampoline are both retired; state derivation remains through `PDERIVE_STATE`. Evidence comes from `tools/include/polycpuid.h`, Bochs CPUID leaf 10, `polyexec` registering only canonical event frames, and the readiness/architecture contract checks. |
| FPGA-facing RTL bring-up artifacts | `rtl/poly_ctrl_decode.sv`; `rtl/poly_frontend_handoff.sv`; `rtl/poly_frontend_decode_dispatch.sv`; `rtl/poly_frontend_fetch_decode_pipeline.sv`; `rtl/poly_frontend_step.sv`; `rtl/poly_frontend_retire.sv`; `rtl/poly_frontend_predecoded_retire.sv`; `rtl/poly_frontend_memory_retire.sv`; `rtl/poly_event_frame_encode.sv`; `rtl/poly_event_frame_stage.sv`; `rtl/poly_frontend_core.sv`; `rtl/poly_frontend_state.sv`; `rtl/poly_frontend_stateful_core.sv`; `rtl/poly_frontend_fpga_top.sv`; `rtl/poly_frontend_fpga_top.xdc`; `rtl/poly_frontend_fetch_issue.sv`; `rtl/poly_x86_fetch_stage.sv`; `rtl/poly_interrupt_boundary.sv`; `rtl/poly_transition_stack.sv`; `rtl/poly_transition_cycle_budget.sv`; `rtl/poly_abi_signature_slots.sv`; `rtl/poly_cpuid_rom.sv`; `rtl/poly_memory_order.sv`; `rtl/poly_raw_data_mem_request.sv`; `rtl/poly_raw_data_mem_response_stage.sv`; `rtl/poly_raw_insn_decode.sv`; `rtl/poly_raw_fetch_request.sv`; `rtl/poly_raw_fetch_response_stage.sv`; `rtl/poly_raw_fetch_stage.sv`; `rtl/poly_raw_fetch_plan.sv`; `rtl/poly_return_cookie_recover.sv`; `rtl/tb_poly_ctrl_decode.sv`; `rtl/tb_poly_raw_fetch_path.sv`; `rtl/tb_poly_frontend_fetch_decode_pipeline.sv`; `rtl/tb_poly_cpuid_rom.sv`; `rtl/tb_poly_frontend_retire.sv`; `rtl/tb_poly_frontend_predecoded_retire.sv`; `rtl/tb_poly_frontend_memory_retire.sv`; `rtl/tb_poly_frontend_core.sv`; `rtl/tb_poly_frontend_stateful_core.sv`; `rtl/tb_poly_frontend_fpga_top.sv`; `rtl/tb_poly_frontend_state.sv`; `rtl/tb_poly_transition_stack.sv`; `rtl/tb_poly_abi_signature_slots.sv`; `rtl/tb_poly_interrupt_boundary.sv`; `rtl/tb_poly_return_cookie_recover.sv`; `rtl/tb_poly_event_frame_stage.sv`; `rtl/tb_poly_memory_order.sv`; `rtl/tb_poly_raw_data_mem_request.sv`; `rtl/tb_poly_raw_data_mem_response_stage.sv`; `make check-poly-rtl` and `make check-poly-rtl-hdl` passed after the RTL event-frame migration, covering Python layout checks, formal memory-order proofs, Icarus simulations, Verilator lint, Yosys process/check, generic synthesis, and Xilinx 7-series out-of-context synthesis (`cells=7208`, `estimated_lcs=3451`). `make poly-rtl-fpga-artifacts` emits and verifies an ignored `out/rtl` handoff bundle containing EDIF, XDC, and a manifest with the current RTL source fingerprint, resource counts, SHA-256 hashes, and `timing_closure=not_run`. |
| Raw frontend memory path | `rtl/poly_raw_fetch_request.sv` validates raw instruction fetch addresses; `rtl/poly_raw_fetch_response_stage.sv` consumes memory responses and blocks instruction retirement on memory faults; `rtl/poly_raw_fetch_stage.sv` keeps a combined request/response bring-up path; `rtl/poly_raw_data_mem_request.sv` validates execute-produced raw data-memory effective addresses and decoded op/width metadata without translation, TLB, or OS policy; `rtl/poly_raw_data_mem_response_stage.sv` converts request-validation errors and memory response faults into precise execute-stage resolved/fault signals. Scalar unaligned raw loads/stores pass through for the downstream memory system to split or fault according to platform policy; atomics require natural alignment at this boundary. |
| Raw instruction classification | `rtl/poly_raw_insn_decode.sv` classifies fetched AArch64/RISC-V loads, stores, atomics, barriers/fences, calls, returns, and traps into hardware sidebands, computes scalar raw memory access byte widths, and computes unconditional direct branch/call targets for immediate-control-flow forms while suppressing unresolved conditional branch targets; `rtl/poly_frontend_decode_dispatch.sv` exposes those sidebands, and `rtl/poly_frontend_core.sv` ORs decoded raw memory categories into the existing x86-TSO retirement gate. |
| Dual frontend fetch issue | `rtl/poly_frontend_fetch_issue.sv` selects the active x86 byte frontend or raw AArch64/RISC-V fetch path from architectural frontend/PC state, validates canonical/range/alignment constraints, and emits one fetch request without decoding or OS policy. |
| Decode dispatch boundary | `rtl/poly_frontend_decode_dispatch.sv` normalizes fetched x86/raw instruction words into Poly control decode inputs, computes raw fallthrough width, and suppresses decode on raw alignment faults without touching memory or OS policy. |
| Fetch/decode pipeline wrapper | `rtl/poly_frontend_fetch_decode_pipeline.sv` composes fetch issue, x86/raw response stages, and decode dispatch into one hardware-facing pipeline boundary with wait/fault/decode sidebands. |
| Frontend fetch-to-retire prototype | `rtl/poly_frontend_memory_retire.sv` feeds `rtl/poly_frontend_fetch_decode_pipeline.sv` into `rtl/poly_frontend_predecoded_retire.sv`, connecting x86/raw fetch issue, response wait/fault handling, decoded control records, and retire/commit/fault outputs without re-running decode. |
| Frontend/transition-stack integration | `rtl/poly_frontend_core.sv` connects precise retired PCALL commits, TSO memory-order backpressure, raw data-memory op metadata and completion/fault gating, raw branch execute sidebands, raw interrupt save/restore, event-frame delivery, ABI signature lookup, CPUID discovery, cycle-budget reporting, execute-resolved raw branch targets, and native return-cookie recovery to `rtl/poly_transition_stack.sv`, blocks PCALL retirement when the transition stack is full or servicing a pop, stalls unresolved raw branch targets until execute resolution, validates resolved raw target canonical/alignment constraints, stalls raw loads/stores/atomics until data-memory completion or fault, exposes raw data-memory load/store/atomic/width/wait/fault and raw branch valid/unresolved/static-target/wait/fault sidebands to an external data path, and keeps return-cookie recovery from colliding with another same-cycle pop. |
| Stateful frontend/PC update boundary | `rtl/poly_frontend_state.sv` validates and applies committed frontend/PC transitions, matching raw interrupt restores, precise trap-vector redirects, trap-return restores, and native return-cookie resumes while holding state on stalls, faults, invalid targets, or same-cycle update conflicts; accepted updates emit a same-cycle frontend/PC redirect with an init/commit/interrupt/trap/return/trap-return reason. |
| Stateful frontend/core composition | `rtl/poly_frontend_stateful_core.sv` feeds `rtl/poly_frontend_core.sv` from architectural frontend/PC state and applies retired commits, decoded raw unconditional direct branch targets, execute-resolved raw branch targets, raw interrupt entry/restore updates, clean event-frame delivery redirects, trap-return restores, and native return-cookie resumes back into `rtl/poly_frontend_state.sv`, while unresolved raw branches, missing trap-return restore state, and raw data-memory accesses hold state until execute-stage resolution; `rtl/poly_frontend_fpga_top.sv` wraps this stateful core with a unified tagged instruction-memory request/response bus plus raw branch/data execute inputs, raw branch metadata outputs, raw data-memory metadata outputs, validated raw data-memory request/response/error outputs, TSO memory-order retirement/backpressure sidebands, fetch/decode fault diagnostics, state validation/error sidebands, raw interrupt entry/restore sidebands, event-frame write envelope/4096-bit payload sidebands, transition-stack pop/error sidebands, native return-cookie resume/error sidebands, trap-vector apply sidebands, trap-return restore sidebands, ABI signature programming/apply sidebands, CPUID discovery sidebands, and transition cycle-budget diagnostics for FPGA integration. |
| Poly control retirement ordering | `rtl/poly_frontend_retire.sv` prevents frontend/PC/transition-stack commits while fetch, execute, or async-boundary handling is not ready, or when older, fetch, execution, or control-validation faults are pending. |
| Hardware-shaped interrupt boundary | `rtl/poly_interrupt_boundary.sv` validates CPL3 raw interrupt entry and matching user-return restore; `rtl/poly_frontend_core.sv` stores the interrupted raw frontend/PC state and blocks same-cycle instruction retirement during interrupt entry or restore; `rtl/poly_frontend_fpga_top.sv` exposes the raw interrupt save frame, restore target, retained interrupted-frame state, and interrupt validation/error sidebands for FPGA integration. |
| Hardware-shaped event delivery | `rtl/poly_event_frame_encode.sv` validates the registered event-frame address as canonical, 64-byte aligned, and non-wrapping for the 512-byte v2 frame, then emits the canonical magic/version/size header, source frontend, event kind, completion/resume metadata, PCs, selector, opaque number, and eight arguments as a 4096-bit little-endian payload. `rtl/poly_event_frame_stage.sv` waits for the event-frame memory write response and suppresses monitor redirect on write faults. |
| Event-frame memory faults | `rtl/poly_event_frame_stage.sv` emits the event-frame write request, waits for memory completion, and reports event-frame write/page faults without delivering the trap; `rtl/poly_frontend_core.sv` blocks same-cycle retirement while the frame write waits or delivers and maps event-frame errors to execute faults. |
| Hardware-shaped native return recovery | `rtl/poly_return_cookie_recover.sv` detects ordinary native returns to the Poly return cookie and requests transition-stack recovery; `rtl/poly_transition_stack_return_formal.sv` proves return-cookie resumes are sourced from the current transition-stack top and are blocked on empty, invalid, or non-cookie cases; `rtl/poly_frontend_fpga_top.sv` exposes the recovered frontend/PC/SP/flags frame, transition pop frame, and return-cookie error diagnostics to the FPGA integration boundary. |
| Fast-path cycle budget | `rtl/poly_transition_cycle_budget.sv` models fixed-cycle `PSWITCH`, register-only `PCALL`, native return-cookie recovery, and event-frame fixed work plus memory-response latency; `rtl/poly_frontend_core.sv` drives that budget model from committed transition, return-cookie, and event-frame events. |
| OS-neutral syscall/libcall boundary | Trap-packet contract in `docs/poly-isa-design-directions.md`; Bochs `handle_poly_syscall_trap`; userspace monitor policy in `tools/runtime/polyexec.c`. |
| Process-mode binfmt handoff | Alpine process-mode binfmt registrations now use `POF` so preserve-argv0, open-binary handoff, and fixed-interpreter behavior are requested together. `polybinfmt-exec` accepts both numeric open-binary fd arguments and `AT_EXECFD`, preserves the fd across `polyexec`, and passes `/proc/self/fd/N` so namespace/chroot launchers do not need the original guest path to be resolvable in the monitor namespace. `boot-poly-alpine-runc-version-smoke` passed with this path. |
| Sustained storage workload | `boot-poly-alpine-sqlite-stress` stages Alpine AArch64 `sqlite`, registers binfmt, and runs a guest-native SQLite WAL transaction/index workload through `polycontainer-run`. The 2026-06-20 boot gate passed with `POLY_ALPINE_SQLITE_STRESS_RESULT: rows=583 hot=71 total=1296978 integrity=ok` and `POLY_ALPINE_SQLITE_STRESS_OK`. |
| Shared-ring I/O workload | `boot-poly-exec-syscall-arch-traps` runs AArch64 and RISC-V `polyexec_process_io_uring_real.c` fixtures that validate NOP, file read/write SQEs, and timeout-remove cancellation completions through shared rings. `boot-poly-alpine-fio-io-uring` stages Alpine AArch64 `fio`, registers binfmt, and runs write/read/randrw jobs with `ioengine=io_uring`; direct I/O is attempted and skipped only if the guest filesystem rejects it. The 2026-06-20 boot gates passed, validating the `PFENCE`/shared-ring handoff path beyond the original small NOP fixture. |
| Explicit per-thread state | Fixed 8KB user state/import image in `tools/include/polycpuid.h`; silicon-facing layout/check program in `tools/programs/polylayout.c`; Bochs state export/import plus `PDERIVE_STATE` in `proc_ctrl.cc`. The legacy real-XSAVE boot gate remains useful bring-up coverage, but is no longer the production OS contract. |
| Native-ABI fast path | ABI signature slots and register maps in `tools/include/polycpuid.h`; `rtl/poly_abi_signature_slots.sv` caches register-only signature slots; `rtl/poly_frontend_core.sv` exposes committed PCALL signature kind/map/TLS metadata for RAT/register-alias application; `rtl/poly_frontend_fpga_top.sv` carries those ABI signature set/apply sidebands to the FPGA boundary; `PCALL` implementation in Bochs; cross-ISA runtime stubs in `tools/runtime/polyexec.c`; Bochs/userspace/nativecheck now verify that `ABI_SIGNATURE_GET` returns the full encoded `kind | (register_map << 32)` value while kind-only helper wrappers preserve existing test compatibility. |
| Complex ABI software path | Stack, aggregate, variadic, import, and syscall policy handled by loader/runtime thunks in `tools/runtime/polyexec.c`, not by hardware descriptors. |
| Native return semantics | Return-cookie transition stacks in Bochs and the spill image; native `ret`/`ret x30`/RISC-V `ret` coverage in `tools/programs/nativecheck.c`. |
| x86 TSO memory-order policy | `tools/include/polycpuid.h` advertises `POLY_MEMORY_MODEL_X86_TSO`; `rtl/poly_memory_order.sv` gates memory-op retirement so foreign frontends do not expose weak reordering and foreign barriers/fences retire as no-ops; `rtl/poly_frontend_core.sv` feeds memory-order waits into precise execute-stage retirement backpressure; `rtl/test_poly_memory_order_litmus.py` checks TSO message-passing and store-buffering outcomes; `rtl/poly_memory_order_formal.sv` captures the intended formal assertions; `make check-poly-rtl-formal` discharges those assertions with Yosys `sat -prove-asserts`. |
| Broad integration validation | `make BOOT_TIMEOUT_SECONDS=900 boot-poly-full-real-xsave-arch-traps` passed on 2026-06-03 for the legacy real-XSAVE bring-up path. On 2026-06-20, `make boot-poly-gdb-core-validation`, `make boot-poly-exec-syscall-arch-traps`, `make boot-poly-alpine-fio-io-uring`, and `make boot-poly-alpine-sqlite-stress` passed. `make check-poly-contracts` gates the zero-kernel state contract, and the focused process/thread plus Postgres/Node/fio/SQLite boot smokes remain the integration gates for real-world userspace behavior. |

## Not Yet Silicon-Complete

- A stateful RTL frontend/PC update boundary, integrated frontend core, and
  FPGA-facing tagged instruction-memory top exist, but there is not yet a full
  RTL/FPGA CPU frontend-switch implementation with production x86/AArch64/RISC-V
  decode engines behind it.
- A directed cycle-budget model exists for the few-cycle `PSWITCH`/`PCALL`
  target, an FPGA-top XDC captures the current bring-up clock/reset/I/O timing
  envelope, and the current RTL technology-maps to Xilinx 7-series cells
  (`cells=7208`, `estimated_lcs=3451`), but no place-and-route
  timing/timing-closure proof exists.
- Bochs proves functional behavior, not timing, area, power, or decode-stage
  feasibility.
- Foreign ISA support is intentionally a documented baseline plus precise traps
  for unsupported AArch64/RISC-V instructions, not a promise of every extension.
- The production OS contract is now zero-kernel-change via canonical event
  frames plus the userspace-offload ISA proposal. Raw-mode page faults use the
  normal monitor-vector/signal path; the old auto-spill descriptor/trampoline
  implementation and its profiling counters have been removed.
- Direct AArch64/RISC-V seccomp policy fixtures pass through `polyexec`, but a
  full Podman container-runtime seccomp profile is not yet a completed gate.
  The first process-mode binfmt entry now uses fd-backed `/proc/self/fd/N`
  handoff and the direct runc-version smoke passes, but Podman still fails later
  when runc invokes the container `init` path as a pathless relative executable
  (`POLYEXEC_FAIL: unable to open init`). Treat that nested runc startup path as
  the remaining enterprise-container blocker before validating full Podman
  seccomp profiles.
- Hardware transition-stack depth, same-cycle push/pop conflicts, underflow,
  overflow, and return-cookie recovery behavior now have directed tests and a
  Yosys temporal-induction proof over a reduced-depth instance; full-depth
  timing/area closure remains unproven.

## Next Engineering Gates

1. Replace the current control-subset x86/raw decode stages with production
   x86/AArch64/RISC-V frontend decoders behind `rtl/poly_frontend_fpga_top.sv`,
   then run timing and resource analysis.
2. For silicon productization, instantiate the frozen CPUID-discovered opcode
   ownership rule with a vendor-owned production page and clear the prototype
   opcode flag in the advertised contract.
