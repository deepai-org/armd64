# Poly Readiness Audit

Date: 2026-06-02

Objective: finish the ISA, Bochs emulator, and userspace runtime enough to have
an FPGA/silicon-ready Poly design for existing precompiled x86_64, AArch64, and
RISC-V64 code.

## Current Evidence

| Requirement | Evidence |
| --- | --- |
| Direct foreign fetch, not per-instruction traps | `docs/poly-isa.md`; Bochs raw AArch64/RISC-V decode in `bochs/cpu/proc_ctrl.cc`; `boot-poly-full-real-xsave-arch-traps` passed. |
| Dedicated Poly control operations | `docs/poly-isa.md`; CPUID geometry in `tools/include/polycpuid.h`; Bochs x86/AArch64/RISC-V control decode in `bochs/cpu/proc_ctrl.cc`. |
| Opcode discovery contract | `tools/include/polycpuid.h` defines CPUID escape subleafs 32 and 33 for active x86 opcode geometry and vendor/prototype opcode-contract flags; Bochs returns both leaves; runtime probes validate both leaves; `rtl/poly_frontend_core.sv` exposes the Poly CPUID ROM through a hardware discovery sideband. The current FPGA bring-up path intentionally keeps `0f 3a fc <subop>` as a CPUID-discovered vendor/prototype dedicated decode page, not a production x86 allocation and not a `#UD` trap envelope. |
| FPGA-facing RTL bring-up artifacts | `rtl/poly_ctrl_decode.sv`; `rtl/poly_frontend_handoff.sv`; `rtl/poly_frontend_decode_dispatch.sv`; `rtl/poly_frontend_fetch_decode_pipeline.sv`; `rtl/poly_frontend_step.sv`; `rtl/poly_frontend_retire.sv`; `rtl/poly_frontend_predecoded_retire.sv`; `rtl/poly_frontend_memory_retire.sv`; `rtl/poly_frontend_core.sv`; `rtl/poly_frontend_state.sv`; `rtl/poly_frontend_stateful_core.sv`; `rtl/poly_frontend_fpga_top.sv`; `rtl/poly_frontend_fetch_issue.sv`; `rtl/poly_x86_fetch_stage.sv`; `rtl/poly_interrupt_boundary.sv`; `rtl/poly_transition_stack.sv`; `rtl/poly_transition_cycle_budget.sv`; `rtl/poly_abi_signature_slots.sv`; `rtl/poly_cpuid_rom.sv`; `rtl/poly_memory_order.sv`; `rtl/poly_raw_data_mem_request.sv`; `rtl/poly_raw_data_mem_response_stage.sv`; `rtl/poly_raw_insn_decode.sv`; `rtl/poly_raw_fetch_request.sv`; `rtl/poly_raw_fetch_response_stage.sv`; `rtl/poly_raw_fetch_stage.sv`; `rtl/poly_raw_fetch_plan.sv`; `rtl/poly_return_cookie_recover.sv`; `rtl/poly_trap_packet_encode.sv`; `rtl/poly_trap_packet_stage.sv`; `rtl/tb_poly_ctrl_decode.sv`; `rtl/tb_poly_raw_fetch_path.sv`; `rtl/tb_poly_frontend_fetch_decode_pipeline.sv`; `rtl/tb_poly_cpuid_rom.sv`; `rtl/tb_poly_frontend_retire.sv`; `rtl/tb_poly_frontend_predecoded_retire.sv`; `rtl/tb_poly_frontend_memory_retire.sv`; `rtl/tb_poly_frontend_core.sv`; `rtl/tb_poly_frontend_stateful_core.sv`; `rtl/tb_poly_frontend_fpga_top.sv`; `rtl/tb_poly_frontend_state.sv`; `rtl/tb_poly_transition_stack.sv`; `rtl/tb_poly_abi_signature_slots.sv`; `rtl/tb_poly_interrupt_boundary.sv`; `rtl/tb_poly_return_cookie_recover.sv`; `rtl/tb_poly_trap_packet_stage.sv`; `rtl/tb_poly_memory_order.sv`; `rtl/tb_poly_raw_data_mem_request.sv`; `rtl/tb_poly_raw_data_mem_response_stage.sv`; `make check-poly-rtl` and `make check-poly-rtl-hdl` passed, including Icarus control-decode, raw-fetch-path, raw-data-memory-request, raw-data-memory-response-stage, frontend-fetch-decode-pipeline, CPUID-ROM, frontend-retire, frontend-predecoded-retire, frontend-memory-retire, frontend-core, frontend-stateful-core, frontend-fpga-top, frontend-state, transition-stack, ABI signature-slot, interrupt-boundary, return-cookie, trap-packet-stage, and memory-order simulations, Verilator lint, Yosys process/check, generic Yosys synthesis, and out-of-context Xilinx 7-series EDIF/resource synthesis for `poly_frontend_fpga_top` (`cells=7143`, `estimated_lcs=3404`). |
| Raw frontend memory path | `rtl/poly_raw_fetch_request.sv` validates raw instruction fetch addresses; `rtl/poly_raw_fetch_response_stage.sv` consumes memory responses and blocks instruction retirement on memory faults; `rtl/poly_raw_fetch_stage.sv` keeps a combined request/response bring-up path; `rtl/poly_raw_data_mem_request.sv` validates execute-produced raw data-memory effective addresses and decoded op/width metadata without translation, TLB, or OS policy; `rtl/poly_raw_data_mem_response_stage.sv` converts request-validation errors and memory response faults into precise execute-stage resolved/fault signals. Scalar unaligned raw loads/stores pass through for the downstream memory system to split or fault according to platform policy; atomics require natural alignment at this boundary. |
| Raw instruction classification | `rtl/poly_raw_insn_decode.sv` classifies fetched AArch64/RISC-V loads, stores, atomics, barriers/fences, calls, returns, and traps into hardware sidebands, computes scalar raw memory access byte widths, and computes unconditional direct branch/call targets for immediate-control-flow forms while suppressing unresolved conditional branch targets; `rtl/poly_frontend_decode_dispatch.sv` exposes those sidebands, and `rtl/poly_frontend_core.sv` ORs decoded raw memory categories into the existing x86-TSO retirement gate. |
| Dual frontend fetch issue | `rtl/poly_frontend_fetch_issue.sv` selects the active x86 byte frontend or raw AArch64/RISC-V fetch path from architectural frontend/PC state, validates canonical/range/alignment constraints, and emits one fetch request without decoding or OS policy. |
| Decode dispatch boundary | `rtl/poly_frontend_decode_dispatch.sv` normalizes fetched x86/raw instruction words into Poly control decode inputs, computes raw fallthrough width, and suppresses decode on raw alignment faults without touching memory or OS policy. |
| Fetch/decode pipeline wrapper | `rtl/poly_frontend_fetch_decode_pipeline.sv` composes fetch issue, x86/raw response stages, and decode dispatch into one hardware-facing pipeline boundary with wait/fault/decode sidebands. |
| Frontend fetch-to-retire prototype | `rtl/poly_frontend_memory_retire.sv` feeds `rtl/poly_frontend_fetch_decode_pipeline.sv` into `rtl/poly_frontend_predecoded_retire.sv`, connecting x86/raw fetch issue, response wait/fault handling, decoded control records, and retire/commit/fault outputs without re-running decode. |
| Frontend/transition-stack integration | `rtl/poly_frontend_core.sv` connects precise retired PCALL commits, TSO memory-order backpressure, raw data-memory op metadata and completion/fault gating, raw branch execute sidebands, raw interrupt save/restore, trap-packet delivery, ABI signature lookup, CPUID discovery, cycle-budget reporting, execute-resolved raw branch targets, and native return-cookie recovery to `rtl/poly_transition_stack.sv`, blocks PCALL retirement when the transition stack is full or servicing a pop, stalls unresolved raw branch targets until execute resolution, validates resolved raw target canonical/alignment constraints, stalls raw loads/stores/atomics until data-memory completion or fault, exposes raw data-memory load/store/atomic/width/wait/fault and raw branch valid/unresolved/static-target/wait/fault sidebands to an external data path, and keeps return-cookie recovery from colliding with another same-cycle pop. |
| Stateful frontend/PC update boundary | `rtl/poly_frontend_state.sv` validates and applies committed frontend/PC transitions, matching raw interrupt restores, precise trap-vector redirects, trap-return restores, and native return-cookie resumes while holding state on stalls, faults, invalid targets, or same-cycle update conflicts; accepted updates emit a same-cycle frontend/PC redirect with an init/commit/interrupt/trap/return/trap-return reason. |
| Stateful frontend/core composition | `rtl/poly_frontend_stateful_core.sv` feeds `rtl/poly_frontend_core.sv` from architectural frontend/PC state and applies retired commits, decoded raw unconditional direct branch targets, execute-resolved raw branch targets, raw interrupt entry/restore updates, clean trap-packet delivery redirects, trap-return restores, and native return-cookie resumes back into `rtl/poly_frontend_state.sv`, while unresolved raw branches, missing trap-return restore state, and raw data-memory accesses hold state until execute-stage resolution; `rtl/poly_frontend_fpga_top.sv` wraps this stateful core with a unified tagged instruction-memory request/response bus plus raw branch/data execute inputs, raw branch metadata outputs, raw data-memory metadata outputs, validated raw data-memory request/response/error outputs, state validation/error sidebands, trap-packet write envelope/payload sidebands, trap-vector apply sidebands, trap-return restore sidebands, ABI signature programming/apply sidebands, and transition cycle-budget diagnostics for FPGA integration. |
| Poly control retirement ordering | `rtl/poly_frontend_retire.sv` prevents frontend/PC/transition-stack commits while fetch, execute, or async-boundary handling is not ready, or when older, fetch, execution, or control-validation faults are pending. |
| Hardware-shaped interrupt boundary | `rtl/poly_interrupt_boundary.sv` validates CPL3 raw interrupt entry and matching user-return restore; `rtl/poly_frontend_core.sv` stores the interrupted raw frontend/PC state and blocks same-cycle instruction retirement during interrupt entry or restore. |
| Hardware-shaped trap delivery | `rtl/poly_trap_packet_encode.sv` emits the 16-qword monitor packet and rejects disabled, non-canonical, unaligned, and boundary-crossing packet addresses before delivery; `rtl/poly_frontend_core.sv` exposes trap-vector apply frontend/PC sidebands only after a clean packet write, and `rtl/poly_frontend_stateful_core.sv`/`rtl/poly_frontend_fpga_top.sv` apply that redirect through the same validated frontend/PC state boundary used by commits, interrupts, returns, and trap-return restores; `rtl/poly_frontend_fpga_top.sv` also exposes the trap-packet write address, byte count, all 16 payload qwords, and detailed validation/error sidebands for FPGA trap-monitor integration. |
| Trap packet memory faults | `rtl/poly_trap_packet_stage.sv` emits the packet write request, waits for memory completion, and reports monitor-packet write/page faults without delivering the trap; `rtl/poly_frontend_core.sv` blocks same-cycle retirement while the packet write waits or delivers and maps packet errors to execute faults. |
| Hardware-shaped native return recovery | `rtl/poly_return_cookie_recover.sv` detects ordinary native returns to the Poly return cookie and requests transition-stack recovery; `rtl/poly_transition_stack_return_formal.sv` proves return-cookie resumes are sourced from the current transition-stack top and are blocked on empty, invalid, or non-cookie cases. |
| Fast-path cycle budget | `rtl/poly_transition_cycle_budget.sv` models fixed-cycle `PSWITCH`, register-only `PCALL`, native return-cookie recovery, and trap-packet fixed work plus memory-response latency; `rtl/poly_frontend_core.sv` drives that budget model from committed transition, return-cookie, and trap-packet events. |
| OS-neutral syscall/libcall boundary | Trap-packet contract in `docs/poly-isa-design-directions.md`; Bochs `handle_poly_syscall_trap`; userspace monitor policy in `tools/runtime/polyexec.c`. |
| Explicit per-thread state | XSAVE-style state layout in `tools/include/polycpuid.h`; silicon-facing layout/check program in `tools/programs/polylayout.c`; guest XCR0 module in `tools/kernel/poly_xcr0.c`; real-XSAVE gates passed. |
| Native-ABI fast path | ABI signature slots and register maps in `tools/include/polycpuid.h`; `rtl/poly_abi_signature_slots.sv` caches register-only signature slots; `rtl/poly_frontend_core.sv` exposes committed PCALL signature kind/map/TLS metadata for RAT/register-alias application; `rtl/poly_frontend_fpga_top.sv` carries those ABI signature set/apply sidebands to the FPGA boundary; `PCALL` implementation in Bochs; cross-ISA runtime stubs in `tools/runtime/polyexec.c`; Bochs/userspace/nativecheck now verify that `ABI_SIGNATURE_GET` returns the full encoded `kind | (register_map << 32)` value while kind-only helper wrappers preserve existing test compatibility. |
| Complex ABI software path | Stack, aggregate, variadic, import, and syscall policy handled by loader/runtime thunks in `tools/runtime/polyexec.c`, not by hardware descriptors. |
| Native return semantics | Return-cookie transition stacks in Bochs and XSAVE state; native `ret`/`ret x30`/RISC-V `ret` coverage in `tools/programs/nativecheck.c`. |
| x86 TSO memory-order policy | `tools/include/polycpuid.h` advertises `POLY_MEMORY_MODEL_X86_TSO`; `rtl/poly_memory_order.sv` gates memory-op retirement so foreign frontends do not expose weak reordering and foreign barriers/fences retire as no-ops; `rtl/poly_frontend_core.sv` feeds memory-order waits into precise execute-stage retirement backpressure; `rtl/test_poly_memory_order_litmus.py` checks TSO message-passing and store-buffering outcomes; `rtl/poly_memory_order_formal.sv` captures the intended formal assertions; `make check-poly-rtl-formal` discharges those assertions with Yosys `sat -prove-asserts`. |
| Broad integration validation | `make BOOT_TIMEOUT_SECONDS=900 boot-poly-full-real-xsave-arch-traps` passed on 2026-06-02. |

## Not Yet Silicon-Complete

- A stateful RTL frontend/PC update boundary, integrated frontend core, and
  FPGA-facing tagged instruction-memory top exist, but there is not yet a full
  RTL/FPGA CPU frontend-switch implementation with production x86/AArch64/RISC-V
  decode engines behind it.
- A directed cycle-budget model exists for the few-cycle `PSWITCH`/`PCALL`
  target and the current RTL technology-maps to Xilinx 7-series cells
  (`cells=7143`, `estimated_lcs=3404`), but no place-and-route
  timing/timing-closure proof exists.
- Bochs proves functional behavior, not timing, area, power, or decode-stage
  feasibility.
- Foreign ISA support is broad enough for current fixtures, but not a complete
  architectural implementation of every AArch64/RISC-V extension.
- OS integration is modeled through an XSAVE-style component and a guest test
  module; no upstream Linux/Windows/macOS kernel support exists.
- Hardware transition-stack depth, same-cycle push/pop conflicts, underflow,
  overflow, and return-cookie recovery behavior now have directed tests and a
  Yosys temporal-induction proof over a reduced-depth instance; full-depth
  timing/area closure remains unproven.

## Next Engineering Gates

1. Replace the current control-subset x86/raw decode stages with production
   x86/AArch64/RISC-V frontend decoders behind `rtl/poly_frontend_fpga_top.sv`,
   then run timing and resource analysis.
2. For silicon productization, replace the vendor/prototype x86 opcode page
   with a production allocation or vendor-owned architectural extension before
   tapeout.
